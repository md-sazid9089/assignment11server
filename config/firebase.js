// Firebase Admin SDK not needed - using client-side Firebase Auth only
const initializeFirebase = () => {
  console.log('Firebase client-side authentication enabled');
};

export default initializeFirebase;
